# GitHub repo “About” (paste into Settings → General → Description)

Use **one short line** here. This repo is the **public site and open clients**, not the closed backend.

## Recommended Description (fits GitHub About field)

**Bonzi Vista public site (Pages): staking mini-app MIT client, slot funnel + waitlist, manuals, ERC-8004 discovery under `.well-known/`. Bot backends live elsewhere.**

## Do **not** use (legacy)

"AI-powered community bot with RAG, leaderboards, and admin dashboard" is legacy copy. That line implies **this repository** ships the bot runtime plus RAG plus dashboards; it does not.

## Optional Topics (Examples)

Suggested: `bonzivista`, `github-pages`, `ethereum`, `static-site`, `erc-8004`, `telegram` (marketing only; not implying full bot source).

## Website URL

Prefer: `https://bonzivista.org`
